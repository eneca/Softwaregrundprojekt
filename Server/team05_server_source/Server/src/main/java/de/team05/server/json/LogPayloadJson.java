package de.team05.server.json;

/**
 * Empty class, because reasons.
 * <p>
 * This class exists purely because the replay decided to contain a single matchFinish ...
 * hm
 * I guess that's how mafia works!
 */
public abstract class LogPayloadJson {
    //empty
}
